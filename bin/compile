#!/bin/bash

echo "-----> Running file extension filter buildpack"

BIN_DIR=$(
	cd $(dirname $0)
	pwd
)
SCRIPT_PATH="$BIN_DIR/steps/apply-file-filter"

BUILD_DIR="$1"

chmod +x "$SCRIPT_PATH"

"$SCRIPT_PATH" "$BUILD_DIR"

echo "-----> File extension filter complete"
