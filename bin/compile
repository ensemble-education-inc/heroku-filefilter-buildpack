#!/bin/bash

echo "-----> Running file extension filter buildpack"

BUILD_DIR="$1"
SCRIPT_PATH="$BUILD_DIR/bin/steps/apply-file-filter"

chmod +x "$SCRIPT_PATH"

"$SCRIPT_PATH" "$BUILD_DIR"

echo "-----> File extension filter complete"
